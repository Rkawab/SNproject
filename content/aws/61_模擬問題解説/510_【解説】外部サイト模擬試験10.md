---
tags: [AWS, SAA, 解説, 模擬試験, Database, DynamoDB, スロットリング, リトライ, Aurora, BlueGreen]
---

# 【解説】外部サイト模擬試験10（[[510_【問題】外部サイト模擬試験10|510]] 対応）

> [!info] このノートの位置づけ
> `51_模擬試験/510_【問題】外部サイト模擬試験10.md` の各問の深掘り解説。**番号・`## 第N問（…）`見出しを問題側と完全一致**させ、Obsidianの見出しリンクで問題⇄解説を相互に飛べるようにしている。StudyNotesアプリの取込対象は問題側のみで、この解説ファイルは閲覧用。

---

## 第1問（Database ｜ 予測不能なスパイクによるDynamoDBスロットリング（オンデマンドモード））

> [!question] 問題（再掲）
> EC2上の注文管理アプリが、在庫データをDynamoDBに保存。テーブルには**読み込み800RCU・書き込み300WCU**をプロビジョニングし、**結果整合性のある読み込み**を実行。**平常時のアクセスは少ない**が、セール開始時やキャンペーン配信直後に**参照と更新が短時間に集中**する。**急増の規模やタイミングを事前に正確に予測するのは困難**。このとき**スロットリングエラー**が発生し、一部ユーザーにエラーページが出ている。最も適切に解決するには？
> - A. DynamoDBテーブルを**オンデマンドモード**に変更する。
> - B. プロビジョンドキャパシティを**2,400RCU / 900WCU へ引き上げる**。
> - C. **エクスポネンシャルバックオフで再試行**するようアプリケーションを設定する。
> - D. **DAXクラスター**を追加し、すべての読み込みをキャッシュ経由にする。

> [!success]- 答え：**A**
> 決め手は問題文の2つの条件。**「急増の規模やタイミングを事前に正確に予測できない」**＝キャパシティを見積もる作戦（B）が原理的に成立しない。**「平常時のアクセス量は少ない」**＝ピークに合わせて常時確保するのは無駄。この両方を同時に解くのが**オンデマンドモード**——事前見積もり不要で自動追随し、使った分だけ課金。

### 用語ミニ辞書（この問題を解くのに必要な語）
- **RCU / WCU（キャパシティユニット）** … DynamoDBの「1秒あたりに何回読み書きしてよいか」の枠。**800RCU＝ざっくり1秒に800回ぶんの読み取り枠**（結果整合性の読みなら1RCUで2回ぶん読めるので実際はもっとお得）、**300WCU＝1秒に300回ぶんの書き込み枠**。
- **プロビジョンドモード** … **枠を事前に自分で決めて確保**する課金方式。使わなくても確保した枠ぶん課金され、**枠を超えたリクエストは断られる**。
- **オンデマンドモード** … **枠を決めない**方式。トラフィックに応じてDynamoDB側が勝手にスケールし、**実際に読み書きしたリクエスト数だけ課金**される。事前のキャパシティ計画が不要。
- **スロットリング** … 確保した枠を超えたときに、DynamoDBがリクエストを**「多すぎます」と断る**こと（`ProvisionedThroughputExceededException`／HTTP 400系）。絞り弁（throttle）で流量を制限するイメージ。
- **結果整合性のある読み込み** … 「書いた直後のごく短い間だけ、古いデータが返ることがあるのを許容する」読み方。強い整合性の読みより**枠の消費が半分で済む＝安い**。
- **DAX（DynamoDB Accelerator）** … DynamoDBの前に置く**読み込み専用のキャッシュ**。同じデータの読み取りをマイクロ秒で返す。**書き込みは高速化しない**。

### ★ご質問の「エクスポネンシャルバックオフ」とは（選択肢Cの正体）

**まず日本語に直すと「指数関数的な待ち時間の後退」＝失敗するたびに、待ち時間を倍々に延ばしながら再試行する方式**です。

リクエストが断られた（スロットリングされた）とき、アプリはすぐ諦めずに「もう一回送ってみる（リトライ）」ことができます。このとき**即座に何度も送り直すと、混んでいる相手をさらに叩くことになって状況が悪化**します。そこで待ち時間を段階的に延ばします。

| 試行 | 失敗後に待つ時間 |
|---|---|
| 1回目が失敗 | 0.1秒待って再送 |
| 2回目も失敗 | 0.2秒待って再送 |
| 3回目も失敗 | 0.4秒待って再送 |
| 4回目も失敗 | 0.8秒待って再送 |

このように**待ち時間を倍々（指数関数的）に増やしていく**ので「エクスポネンシャル（指数的）バックオフ（後退・引き下がる）」と呼びます。混雑している相手に対して**だんだん遠慮していく**イメージです。

補足として、実際には**ジッター**（待ち時間に少しランダムな揺らぎを加える工夫）も併用します。全クライアントが同じ0.2秒後に一斉再送すると、また同時に殺到してしまうためです。

**そして重要なのは、これは「悪い方法」ではないこと**。むしろ**AWS SDKが既定で自動的にやってくれるベストプラクティス**で、DynamoDBを使うなら当然入っている前提の仕組みです。この選択肢が不正解なのは「間違った技術だから」ではなく、**この問題の要件を解決しないから**です（次項）。

### 各選択肢をズバッと（不正解がなぜダメか）
- **A：正解** … オンデマンドモードは**キャパシティ計画そのものを不要にする**。「予測できない」という問題文の核心を、**予測しなくてよくする**ことで正面から解決する。急増には自動追随し、平常時が少ないので**使った分だけ課金**はコスト面でも噛み合う。
- **B：誤り** … 2,400RCU / 900WCU（現状の3倍）へ引き上げる案。2つの理由で×。①**平常時のアクセスは少ない**のに、ピーク前提の枠を24時間365日確保して払い続ける＝**無駄なコスト**。②そもそも**「規模を事前に正確に予測することは困難」**と問題文が明言しているので、**3倍で足りる根拠がない**。次のセールが想定を超えれば**また同じスロットリングが起きる**。予測できない問題に固定値で対処する、典型的な対症療法。
- **C：誤り** … エクスポネンシャルバックオフ自体は正しいベストプラクティスだが、**やっているのは「断られたら少し待ってもう一度頼む」だけ**。**枠が足りないという原因は1ミリも解消しない**。急増が数分続けば、待って再送しても枠は依然埋まっているので**再試行も失敗し、最終的にエラーページは出る**。うまくいっても**応答が遅くなる**だけで、ユーザー体験は改善しない。**「エラーを減らす緩和策」であって「問題の解決」ではない**——設問は「最も適切に**解決**する」と聞いているのでここで落ちる。
- **D：誤り（最大の罠）** … DAXは**読み込み専用のキャッシュ**。ところが問題文は「多数の商品に対する**参照と更新**が短時間に集中」と書いており、**書き込み（更新）のスロットリングをDAXは一切解決できない**（書き込みは結局DynamoDB本体へ行きWCUを消費する）。読み込み側が改善しても**書き込みでエラーが出続ける**ので、問題は解決しない。「読み込みが多い→キャッシュ」と反射的に選ぶと外す。

> [!tip] SAA着眼点
> - **「トラフィックが予測できない／急激で不規則なスパイク」＋「平常時は少ない」→ DynamoDB オンデマンドモード** が定番解。逆に**「安定して予測可能な負荷」ならプロビジョンド**（そちらが安い）。この対比は頻出。
> - **「予測が困難」と書いてあったら、キャパシティを固定値で引き上げる選択肢は基本×**。予測不能な問題に固定値は当たらない。
> - **リトライ／エクスポネンシャルバックオフは「緩和策」であって「根本解決」ではない**。「最も適切に解決する」と聞かれたら、原因（＝キャパシティ不足）を潰す選択肢を選ぶ。ベストプラクティスだからといって正解にならない典型パターン。
> - **DAXは読み込み専用**。問題文に「更新」「書き込み」が混ざっていたらDAXでは解決しない。
> - 補足：プロビジョンドのまま粘るなら **Auto Scaling** という手もあるが、**急激なスパイクには追従が間に合わない**（スケールに数分かかる）ため、瞬間的な急増ではオンデマンドが勝る。

### 関連
- 学習ノート：[[0303_DynamoDB]] / [[0304_ElastiCache]]
- 用語ドリル：[[609_【問題】読解・用語ドリル9]]（リトライ・キャパシティ編）
- 元問題：[[510_【問題】外部サイト模擬試験10#第1問（Database ｜ 予測不能なスパイクによるDynamoDBスロットリング（オンデマンドモード））]]

---

## 第2問（Database ｜ スキーマ変更の本番影響ゼロ検証と低停止切替（Aurora Blue/Greenデプロイ））

> [!question] 問題（再掲）
> ECサイト企業が商品管理アプリのDBに**Aurora PostgreSQL**を使用。**テーブル定義の変更やインデックス追加を含むスキーマ更新**を検証したい。検証は**本番に影響を与えない**状態で行い、**検証後は本番へ反映**、かつ**停止時間は最小**にしたい。最も適切な方法は？
> - A. **Auroraレプリカ**を作り、レプリカ側でスキーマ変更・検証。問題なければレプリカを**昇格**させ本番にする。
> - B. 本番の**スナップショット**から検証用DBクラスターを作成。検証後、アプリの**接続先を手動で変更**。
> - C. Aurora で**ブルー/グリーンデプロイ**を作成し、別途グリーン環境で検証。問題なければ**スイッチオーバー**でグリーンを新本番にする。
> - D. 本番クラスターに**メンテナンスウィンドウ中に直接**変更を適用。失敗時はバックアップから復元。

> [!success]- 答え：**C**
> 要件は3つ——「**①本番に影響を与えず検証**」「**②検証後に本番へ反映**」「**③停止時間は最小**」。この3つを1つの機能で満たす専用サービスが**Aurora のブルー/グリーンデプロイ**。本番のそっくりなクローンを別に作り、**裏で同期を保ちながら**そこで安全に検証し、OKなら**スイッチオーバー（切り替え）で数分・低リスクに本番を入れ替える**。

この問題は「本番を触らずに試して、うまくいったら丸ごと入れ替えたい」という**"リハーサル付きの引っ越し"**をどう実現するか、の問題です。用語（レプリカ／スナップショット／ブルー・グリーン／スイッチオーバー）が似ていて紛らわしいので、まず1つずつ意味を押さえます。

### 用語ミニ辞書（この問題の紛らわしい4語）
- **スキーマ** … データベースの**構造・設計図**のこと。「どんなテーブルがあり、どの列を持ち、どこに索引（インデックス）を張るか」。今回はこの設計図を変えたい＝**構造変更**なので、下手をすると本番が壊れる。だから事前検証が要る。
- **インデックス** … 本の巻末索引と同じで、検索を速くするための仕組み。追加すると検索は速くなるが、貼る作業中にテーブルがロックされる等、本番に影響が出ることがある。
- **Auroraレプリカ** … 本番クラスターの**読み取り用のコピー**。常に本番と同じ内容へ**自動同期**され、読み取り負荷の分散や、本番が落ちたときの**昇格（フェイルオーバー）**に使う。あくまで「本番と一心同体の分身」で、**独立した実験場ではない**。
- **スナップショット** … ある**一瞬のデータの丸ごとバックアップ（写真）**。そこから別クラスターを復元できるが、**撮った瞬間で時間が止まっている**——以後の本番の更新は反映されない。
- **ブルー/グリーンデプロイ** … 「**今の本番＝ブルー**」と「**変更を入れた新環境＝グリーン**」の**2つを並行に用意**し、グリーンで検証してから一気に切り替える手法。Auroraはこれを専用機能として持ち、**グリーンをブルーに追従して同期し続ける**のがミソ（スナップショットと違い時間が止まらない）。
- **スイッチオーバー** … ブルー/グリーンで、**接続先をブルーからグリーンへ切り替える操作**。Auroraが同期の完了を待って安全に行うので、**ダウンタイムは数分程度**で済む。
- **メンテナンスウィンドウ** … AWSが設定する「メンテしてよい時間帯」。Dはこの時間に**本番へ直接**変更を当てる案。

### なぜCが3要件すべてに刺さるのか
- **①本番に影響を与えず検証** → グリーンは本番（ブルー）とは**別の環境**。ここでスキーマを壊しても本番は無傷。
- **②検証後に本番へ反映** → ブルーとグリーンは**レプリケーションで同期**され続けるので、検証中に入った本番の新しい注文データもグリーンに反映され、切替時にデータが古くならない。
- **③停止時間は最小** → **スイッチオーバー**はAuroraが同期完了を確認して切り替えるため、**数分程度の短い停止**で済む。

### 各選択肢をズバッと（不正解がなぜダメか）
- **C：正解** … Blue/Greenは「同期を保った隔離環境で検証 → 低停止・低リスクで切替」を1機能で提供。3要件に完全一致。スキーマ変更の検証・適用の定番解。
- **A：誤り** … Auroraレプリカは**読み取り分散・可用性のための"本番の分身"**であって、独立した実験場ではない。レプリカは本番と密結合で同期しているため、そこにスキーマ変更を加える運用は想定外だし、**「本番に影響を与えない」も担保できない**。レプリカの「昇格」は本番障害時のフェイルオーバー用語で、検証済み環境への切替とは意味が違う。用語の混同を突く誤答。
- **B：誤り** … スナップショット複製は「本番に触らず検証」まではできる。しかし**スナップショットは撮った瞬間で時間が止まる**ため、検証中に本番へ入った新規データが検証用DBに反映されない＝**切替時にデータが古くなる**。さらに接続先を手動で切り替えるので、切替の停止時間やデータ差分のリスクがBlue/Greenより大きい。「同期され続けるか」がCとの決定的な差。
- **D：誤り（最大の罠）** … **本番へ直接変更を当てる**時点で「**本番に影響を与えない**」という要件に真っ向から反する。しかも失敗したらバックアップから復元＝**長時間ダウン＋復元時点以降のデータ損失**。「停止最小」とも正反対。一番やってはいけない選択肢。

> [!tip] SAA着眼点
> - **「本番に影響を与えずスキーマ/エンジン変更を検証」＋「低ダウンタイムで切替」→ RDS/Aurora のブルー/グリーンデプロイ** が定番解。DBのメジャーバージョンアップやスキーマ変更のシナリオで頻出。
> - **Blue/Greenの肝は「グリーンが本番に同期され続ける」こと**。スナップショット複製（時間が止まる）との違いがそのまま正誤の分岐。
> - **Auroraレプリカ＝読み取り分散・可用性（本番の分身）**、**Blue/Green＝検証・切替（別環境）**。役割が別物。「レプリカで検証」は用語の混同を狙う誤答。
> - **「本番に直接変更を当てる」「失敗したらバックアップから復元」は"本番影響なし/低停止"の要件でほぼ常に×**。

### 関連
- 学習ノート：[[0307_Aurora詳細]] / [[0301_RDS]]
- 元問題：[[510_【問題】外部サイト模擬試験10#第2問（Database ｜ スキーマ変更の本番影響ゼロ検証と低停止切替（Aurora Blue/Greenデプロイ））]]
