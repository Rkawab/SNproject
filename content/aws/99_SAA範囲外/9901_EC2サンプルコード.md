---
tags: [AWS, EC2, SAA範囲外]
---

> [!warning] SAA対象外
> このノートは EC2 UserData で配置する HTML/CSS のサンプルそのもので、**SAA試験の出題範囲ではありません**（実務・ハンズオン用メモ）。試験対策としては読み飛ばしてOK。
> 判定の根拠・基準は [[SAA範囲の取捨選択メモ]] を参照。

```md
#!/bin/bash
sudo su -
dnf update -y
dnf install -y httpd

# HTMLページを作成
cat > /var/www/html/index.html << 'EOF'
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My EC2 Server</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Noto+Sans+JP:wght@300;400&display=swap');

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:      #0a0e1a;
      --surface: #111827;
      --border:  #1e2d45;
      --accent:  #00e5ff;
      --green:   #00ff88;
      --text:    #c9d6e3;
      --dim:     #4a6080;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Noto Sans JP', sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      /* グリッドライン背景 */
      background-image:
        linear-gradient(var(--border) 1px, transparent 1px),
        linear-gradient(90deg, var(--border) 1px, transparent 1px);
      background-size: 40px 40px;
    }

    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 2.5rem 3rem;
      max-width: 560px;
      width: 100%;
      position: relative;
      box-shadow: 0 0 60px rgba(0,229,255,0.06);
      animation: fadeIn 0.8s ease both;
    }

    /* 左上のアクセントライン */
    .card::before {
      content: '';
      position: absolute;
      top: -1px; left: -1px;
      width: 60px; height: 4px;
      background: var(--accent);
      border-radius: 4px 0 0 0;
    }

    /* ステータスバッジ */
    .status {
      font-family: 'Space Mono', monospace;
      font-size: 0.7rem;
      color: var(--green);
      display: flex;
      align-items: center;
      gap: 6px;
      margin-bottom: 1.8rem;
      letter-spacing: 0.1em;
    }
    .status-dot {
      width: 7px; height: 7px;
      border-radius: 50%;
      background: var(--green);
      box-shadow: 0 0 8px var(--green);
      animation: blink 1.6s ease-in-out infinite;
    }

    h1 {
      font-family: 'Space Mono', monospace;
      font-size: 1.9rem;
      font-weight: 700;
      color: #fff;
      line-height: 1.2;
      margin-bottom: 0.5rem;
    }
    h1 span { color: var(--accent); }

    .subtitle {
      font-size: 0.85rem;
      color: var(--dim);
      font-weight: 300;
      margin-bottom: 2rem;
    }

    /* メタ情報グリッド */
    .meta {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1px;
      background: var(--border);
      border: 1px solid var(--border);
      border-radius: 3px;
      overflow: hidden;
      margin-bottom: 2rem;
    }
    .meta-item {
      background: var(--surface);
      padding: 0.75rem 1rem;
    }
    .meta-label {
      font-family: 'Space Mono', monospace;
      font-size: 0.62rem;
      color: var(--dim);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      margin-bottom: 3px;
    }
    .meta-value {
      font-family: 'Space Mono', monospace;
      font-size: 0.8rem;
      color: var(--accent);
    }

    /* コマンドブロック */
    .cmd {
      background: #070b13;
      border: 1px solid var(--border);
      border-left: 3px solid var(--accent);
      border-radius: 3px;
      padding: 1rem 1.2rem;
      font-family: 'Space Mono', monospace;
      font-size: 0.78rem;
      color: var(--green);
      line-height: 1.8;
    }
    .cmd .prompt { color: var(--dim); user-select: none; }

    /* フッター */
    .footer {
      margin-top: 1.8rem;
      font-size: 0.72rem;
      color: var(--dim);
      display: flex;
      justify-content: space-between;
      font-family: 'Space Mono', monospace;
    }

    @keyframes blink {
      0%, 100% { opacity: 1; }
      50%       { opacity: 0.3; }
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(16px); }
      to   { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="status">
      <span class="status-dot"></span>
      INSTANCE RUNNING
    </div>

    <h1>Hello, <span>World!</span></h1>
    <p class="subtitle">Amazon EC2 — Apache Web Server</p>

    <div class="meta">
      <div class="meta-item">
        <div class="meta-label">Web Server</div>
        <div class="meta-value">Apache httpd</div>
      </div>
      <div class="meta-item">
        <div class="meta-label">Platform</div>
        <div class="meta-value">Amazon Linux 2023</div>
      </div>
      <div class="meta-item">
        <div class="meta-label">Service</div>
        <div class="meta-value">systemd enabled</div>
      </div>
      <div class="meta-item">
        <div class="meta-label">Provisioned by</div>
        <div class="meta-value">User Data</div>
      </div>
    </div>

    <div class="cmd">
      <span class="prompt">$ </span>systemctl status httpd<br>
      <span class="prompt">● </span>httpd.service — active (running)
    </div>

    <div class="footer">
      <span>EC2 Study Lab</span>
      <span>HTTP :80</span>
    </div>
  </div>
</body>
</html>
EOF

systemctl start httpd
systemctl enable httpd
```

