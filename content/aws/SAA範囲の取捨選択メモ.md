---
tags: [AWS, SAA, メタ, 学習方針]
---

# SAA範囲の取捨選択メモ（過剰／不足の調査結果）

> [!info] このノートの目的
> `03_Notes/02_学習/AWS/` 配下の各ノートを **SAA（Solutions Architect – Associate）対策** の観点で点検し、「**過剰（試験範囲外・深すぎ）**」「**不足（頻出なのに無い）**」を記録したもの。
> 同じ点検を繰り返さないための基準書。
> **最終整理日：2026-06-07**（全ファイルを学習順に2桁番号付け＋SAA範囲外を `99_SAA範囲外/` へ退避＋リンク全張り直しを実施）。

---

## 📂 フォルダ運用ルール（2026-06-07 整理で確立）

> [!note] このフォルダの構造
> - 各サブフォルダ（`01_EC2` 〜 `15_移行と転送`）内のファイルは、**学習順に2桁の連番**（`01_`〜）を付与。試験解説を依頼する際に参照ファイルを特定しやすくするため。
> - **SAA範囲外**と判定したノートは `99_SAA範囲外/` に退避（本体フォルダは試験範囲に集中）。
> - **SAA深掘り**（範囲内だが深すぎ）はフォルダに残し、frontmatterの `SAA深掘り` タグ＋冒頭 `[!note]` で明示。
> - Obsidianの `[[...]]` リンクはファイル名（番号付き）で張る。

---

## 判定基準

| 区分 | 定義 | 対応 |
|---|---|---|
| **SAA範囲外** | AWSと無関係 or 試験にまず出ない実装詳細（DBエンジン内部・HTML等） | **`99_SAA範囲外/` へ移動** ＋ `SAA範囲外` タグ＋冒頭 `[!warning]` |
| **SAA深掘り** | サービス自体は範囲だが、SAAに必要な深さを超える内部解説 | フォルダに残し `SAA深掘り` タグ＋冒頭 `[!note]` |
| **不足** | SAA頻出だが専用ノートが無い／薄い | 本メモに記録（**勉強中につき今後作成予定 → 当面は対応保留**） |

> [!tip] タグでまとめて確認
> Obsidianのタグ検索で `#SAA範囲外` `#SAA深掘り` を引くと、試験対策から外してよいノートが一覧できる。

---

## 🟡 過剰

### SAA範囲外（`99_SAA範囲外/` へ退避済み）

- [[9901_EC2サンプルコード]] … UserDataのHTML/CSS全文。実務メモ。
- [[9902_MySQLとPostgreSQL]] … MVCC・VACUUM・ファイル構造などエンジン内部。アプリ開発寄り。
- [[9903_IPアドレス・グローバルIP]] … 家庭回線（CGN・IPoE・ポート開放）の話。AWSと無関係。

### SAA深掘り（範囲だが深すぎ・参考レベル。フォルダに残置）

- [[0908_Kubernetes]] … etcd等の内部コンポーネント・マニフェスト。SAAは「EKS＝マネージドK8s」で十分。
- [[0909_ECSエージェントとコントロールプレーン]] … SSM/ECS Agentの内部通信。
- [[0907_EKS_Distro]] … 単体出題ほぼ無し。EKSシリーズ比較の一部で可。
- [[0707_VMware_Cloud_on_AWS]] … サービスは範囲内だが前半のVMware歴史・仮想化背景が冗長。

---

## 🔴 不足（SAA頻出だが未整備）※勉強中につき今後追記予定・当面保留

> [!note] 方針
> これらは現在学習中で、今後ノート化する可能性が高いため**当面は対応を見送り**。作成したらこのリストから消す。

優先度順：

1. **Lambda**（同時実行・コールドスタート等の専用ノート）。※比較は [[0702_Lambda・ECS・RDS・Beanstalk比較]] にあり
2. **KMS**（S3暗号化で断片的にあるのみ → [[0207_S3の暗号化]]）
3. **EBS ボリュームタイプ**（gp3/io2/st1/sc1）の単体ノート。※概要は [[0103_EC2のストレージ]]
4. **Aurora 詳細**（Serverless / Global Database）。※基本は [[0301_RDS]] 内

> [!success] VPCコア（旧不足#1）は解消済み
> Security Group vs NACL（[[0408_セキュリティグループとNACL]]）に続き、**NAT Gateway・Internet Gateway・VPCエンドポイント（Gateway/Interface型）** も [[0409_IGW・NAT Gateway・VPCエンドポイント]] で整理し、VPCコアの不足は全て埋まった（2026-06-16）。Transit Gateway/PrivateLinkは [[0405_ネットワークとコンテンツ配信]] でカバー済み。

> [!tip] 2026-06-07時点でカバー済みになった旧「不足」項目
> - **CloudFront / Direct Connect / VPN / Transit Gateway / PrivateLink / Global Accelerator** → [[0405_ネットワークとコンテンツ配信]]
> - **Storage Gateway / DataSync / Snow Family** → [[1501_移行と転送サービス一覧]] / [[1502_DataSync vs Firehose vs Storage Gateway]]
> - **WAF / Shield / GuardDuty / Inspector / Macie / KMS / Cognito 等セキュリティ全般** → 新フォルダ `16_セキュリティ・アイデンティティ・コンプライアンス/`（[[1601_セキュリティサービス一覧]]）にて整理済み（2026-06-07）

---

## ✅ 修正済み

### 2026-06-16
- 不足リスト最優先（VPCコア）の残り **NAT Gateway・Internet Gateway・VPCエンドポイント** を解消 → [[0409_IGW・NAT Gateway・VPCエンドポイント]] を新規作成（IGW＝双方向の玄関／NAT GW＝片方向の出口・AZごと冗長・NATインスタンス比較／VPCエンドポイントのGateway型(S3・DynamoDB・無料)vs Interface型(PrivateLink・有料)、決定フローとコスト最適化の典型問題を整理）。これでVPCコアの不足は全て解消。

### 2026-06-15
- 不足リスト最優先（VPCコア）の **Security Group vs NACL** を解消 → [[0408_セキュリティグループとNACL]] を新規作成（適用単位／ステートフル・レス／許可・拒否／評価順／デフォルト挙動の対比表、SAA出題パターン早見を整理）。[[1604_ネットワーク防御]] のリンクも本ノートへ張り替え。

### 2026-06-11
- 不足リストの **Redshift** を解消 → [[1113_Amazon Redshift]] を新規作成（Redshift Spectrum・Athenaとの違い・OLTP/OLAP比較・可用性の注意点を整理）。[[1101_AWS分析サービス一覧]] の目次にも追加。

### 2026-06-07
- [[0301_RDS]]：リードレプリカ「最大5台」→ **最大15台**（Oracle/SQL Serverは5台）に修正済み。
- [[0103_EC2のストレージ]]：容量管理の行「**ら**✅ 自動伸縮」の誤字を修正済み。
- Kinesisノートが2つある件（[[0805_Kinesis]]＝08_アプリ統合の詳細版 と [[1103_Amazon Kinesis]]＝11_分析の入門版）→ **役割分担済み**（相互リンクで明記）。

## ⚠️ 残課題（気づいたら対応）

- [[0402_プロトコル]]：7行のみで中途半端（SAA重要度は低いので優先度低）。

---

## 関連
- [[AWS学習のコツ_用語整理術]]
