[[0201_S3]]

# S3 vs EBS / EFS の違い

- **EBS** = ブロックストレージ。EC2にマウントして**ディスク**として使う（基本1台のみ）
- **EFS** = ファイルストレージ（NFS）。**複数EC2から共有フォルダ**としてマウント
- **S3** = オブジェクトストレージ。マウントせず **HTTP API・URLでアクセス**
- **Amazon FSx for Windows File Server** = Windows向けファイルストレージ（SMB/NTFS/AD連携）。**Windowsの共有フォルダ**として使う

> [!tip] 例え：Windowsでの対応
> | AWS | Windowsの例え | 用途 |
> |---|---|---|
> | EBS | **Cドライブ**（内蔵SSD） | OSやアプリが動く場所 |
> | EFS | **Linux向けネットワークドライブ**（NFS） | 複数Linuxサーバーで共有する社内フォルダ |
> | FSx for Windows File Server | **Windowsの共有フォルダ**（SMB） | Windowsアプリ・AD連携が必要な共有 |
> | S3 | **GoogleドライブやOneDrive** | ブラウザ・APIでアクセスするファイル置き場 |

## 詳細比較表

| 項目 | EBS | EFS | S3 |
|---|---|---|---|
| ストレージ種別 | ブロック | ファイル（NFS） | オブジェクト |
| アクセス方法 | EC2にマウント | EC2にマウント | HTTP/HTTPS（API・URL） |
| 複数EC2から同時利用 | ❌（基本1台） | ✅ | ✅（マウント不要） |
| 対応OS | Linux / Windows | Linuxのみ | 関係なし |
| AZまたがり | ❌ 同一AZのみ | ✅ 複数AZ | ✅ 複数AZ（自動） |
| 容量管理 | △ 手動で事前指定 | ✅ 自動伸縮 | ✅ 無制限 |
| 耐久性 | 通常 | 高い | ◎ イレブンナイン（99.999999999%） |
| バックアップ | スナップショット（S3に保存） | AWS Backup対応 | バージョニング・レプリケーション |
| コスト | ○ 安い | ❌ 高い（EBSの約3倍） | ○ 安い（容量単価最安） |
| 主な用途 | OS・DB・本番データ | 共有コンテンツ・ホームDir | 静的ファイル・バックアップ・データレイク |

## アクセスイメージ

```
【EBS】
EC2 ── マウント ──→ EBS（ディスクとして使う）

【EFS】
EC2 ┐
EC2 ├── マウント ──→ EFS（共有フォルダとして使う）
EC2 ┘

【S3】
EC2        ┐
ブラウザ    ├── APIリクエスト ──→ S3（URLでアクセス）
スマホアプリ ┘
```

## S3が「ディスクの代わり」にならない理由

S3はマウントされないため、ファイルの直接編集はできず「**ダウンロード → 編集 → 再アップロード**」になる。

| できないこと | 理由 |
|---|---|
| EC2のOSとして使えない | マウントできないので起動ディスクにならない |
| DBのデータ置き場にできない | MySQLなどはブロックストレージが必須 |
| ファイルの一部だけ書き換えできない | オブジェクト単位での上書きのみ（全体の再アップロードが必要） |
| 低レイテンシが必要な処理に使えない | APIアクセスなのでEBSより遅い |

## なぜS3はEFSより安いのか

| サービス | 理由 |
|---|---|
| EFS | 即座にマウントして読み書きできる状態を**常に維持**している → 高い |
| S3 | API経由の取り出しに少し時間がかかってもいい設計 → 安い |

## FSx for Windows File Server はいつ出る？

**Amazon FSx for Windows File Server** は、Windows Server 上に構築されたフルマネージドなファイル共有サービス。SMBプロトコル、NTFSアクセス権、Active Directory連携が必要なときに使う。
FSxには他に **Lustre（HPC・ML向け高速）/ NetApp ONTAP / OpenZFS** の3種があり、4種の見分け方は → [[0213_FSxファミリー4種の使い分け]]

| 要件 | 選ぶもの |
|---|---|
| Linuxの複数EC2で共有ファイル | **EFS** |
| WindowsアプリがSMB共有・NTFS権限・AD連携を必要とする | **FSx for Windows File Server** |
| ファイル共有ではなく、URL/APIでオブジェクトを保存 | **S3** |

> [!tip] SAAひっかけ
> 「複数EC2で共有」だけならEFSを選びがちだが、問題文に **Windows / SMB / NTFS / Active Directory** が出たら FSx for Windows File Server を疑う。

## SAA試験ポイント

| 問題文のキーワード | 答え |
|---|---|
| EC2の起動ディスク・DBのデータ領域 | **EBS** |
| 複数AZの複数EC2から共有（Linux） | **EFS**（設定詳細 → [[0214_EFS詳細]]） |
| Windowsアプリの共有ファイル、SMB、NTFS、AD連携 | **FSx for Windows File Server** |
| 静的コンテンツ・バックアップ・データレイク・容量無制限 | **S3** |
| 「最も耐久性が高いストレージ」 | **S3**（イレブンナイン） |
| 「最もコスト効率の良い大容量保存」 | **S3**（＋ストレージクラス → [[0203_S3ストレージクラス]]） |

## まとめ

```
EBS / EFS → ドライブ（マウントして使う）
S3        → ファイル置き場（API・ブラウザ経由）
```

> S3は「便利で安い」のではなく「**用途が違う**」。ディスクの代わりにはならない、あくまでファイル置き場。
